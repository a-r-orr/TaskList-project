<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head', { title: 'Welcome' }); %>
    <link rel="stylesheet" href="/css/login.css">
</head>

<body>

    <div class="login-container">
        <div class="login-wrap tl-rounded">
            <img src="/images/TaskListLogo.png" alt="" id="logo">
            <h3 class="login-form-title">Welcome</h3>
            <% if (returnMessage.length > 0) { %>
                <p class="return-message"><%= returnMessage %></p>
            <% } %>
            <form method="post" action="/login" class="login-form">
                <div class="wrap-login-input">
                    <input type="text" name="email" placeholder="Email or Username" class="login-form-input" required>
                </div>
                <div class="wrap-login-input">
                    <input type="password" name="password" placeholder="Password" class="login-form-input" required>
                </div>
                <button class="login-button">Login</button>
            </form>
            <p class="login-no-account">Don't have an account yet? <a href="/register">Sign Up</a></p>
        </div>
    </div>

</body>

</html>